# NAS Archive Native 2.0.1 — إصلاح مشكلات فحص الماسح ومعاينة مستندات PDF

الإصدار التحديثي **NAS Archive Native 2.0.1** لمعالجة وتثبيت استقرار فحص الماسح الضوئي الفيزيائي وتوليد ومعاينة مستندات PDF بدقة وأمان كامل دون أي اعتماد خارجي.

---

### 📦 حزمة التثبيت الرسمية وبيانات التحقق (Official Installer)

| الملف | الوصف | الحجم | بصمة التشفير (SHA-256) |
| :--- | :--- | :---: | :--- |
| **`NAS Archive Setup 2.0.1.exe`** | مثبت نظام Windows 11 x64 المستقل (NSIS) | 122.45 MB | `B51CEE96B487724D75F4F1D1029615BFB85247E4296FC1D08AC3FFAF1D23F433` |

---

### 🔍 تحليل الأسباب الجذرية والإصلاحات البرمجية (Root Cause Analysis & Fixes)

1. **إصلاح مسار مساحة المعاينة المؤقتة (Staging Directory Resolution)**:
   - **السبب**: في النسخة المثبتة المجمعة، كان المسار المؤقت يشير إلى مجلد افتراضي داخل مجلد الحزمة `resources/app.asar/runtime/staging` وهو غير موجود، بينما كانت عملية المسح الفيزيائي الناجحة تحفظ ملف PDF الصالح داخل مجلد بيانات المستخدم `AppData\Roaming\nas-archive\storage\staging`. وعند محاولة الواجهة قراءة الملف عبر IPC، كانت النتيجة إرجاع خطأ "المستند غير موجود في مساحة المعاينة المؤقتة".
   - **الإصلاح**: توحيد مسار `staging` في قنوات الـ IPC (`staging:read` و `staging:validate`) ليعتمد بشكل مباشر ومطلق على مسار `storage.dirs.staging` الموثوق والمعرف في النواة المحلية، مع فحص وجود الملف وحجمه والتحقق الصارم من ترويسة `%PDF-`.

2. **تصحيح فحص جاهزية الماسح الضوئي (Scanner Readiness vs Device Discovery)**:
   - **السبب**: اختلاف في مفاتيح كائن الاستجابة في قنوات IPC بين `devices` و `detected_devices`، مما جعل الواجهة تقرأ كائن الأجهزة كـ `undefined` وتبقي شارة الماسح في حالة التحذير `فحص الماسح...`.
   - **الإصلاح**: إضافة دالة فحص الجاهزية `checkReadiness` في `electron/core/scanner.js` للتفريق بين مجرد الكشف الاسمي عن الجهاز والجاهزية الفعلية لبدء المسح، وتوحيد إرجاع `devices` و `detected_devices` و `ready: true/false`.

3. **إزالة مؤشرات Paperless القديمة من الواجهة**:
   - **الإصلاح**: استبدال مؤشر الحالة القديم `Paperless متصل` بـ `المحرك المحلي متصل` في شريط الحالة العلوي ليعكس هوية التطبيق المستقل بنسبة 100%.

4. **حماية حالة المستند وزر الأرشفة عند إعادة المسح (Rescan Resilience & Button Guard)**:
   - **السبب**: كان زر الأرشفة يُعاد تفعيله بصورة غير مشروطة في كتلة `finally` حتى عند فشل إعادة المسح، مما يتيح محاولة أرشفة ملف غير صالح أو مفقود.
   - **الإصلاح**: الاحتفاظ بالوثيقة الصالحة السابقة واستعادتها فوراً في حالة فشل إعادة المسح مع إعادة تحميل المعاينة السابقة، وإبقاء زر "أرشفة الوثيقة" معطلاً ومحجوباً بتأثير CSS صارم حتى يتم التحقق الفعلي من سلامة المستند.

---

### 🧪 التحقق والاختبارات الآلية (Verification & Test Results)

- **جناح اختبارات الانحدار للمسح والمعاينة (8/8 بنسبة 100%)**:
  1. معالجة المستندات المفقودة ورفضها بأمان.
  2. فحص ترويسة PDF واكتشاف الملفات التالفة بدقة.
  3. دعم الأسماء العربية والرموز الخاصة والترميز النصي للروابط.
  4. فحص جاهزية الماسح الضوئي والتعامل مع عدم توفره.
  5. الاحتفاظ بالوثيقة السابقة الصالحة عند فشل إعادة المسح.
  6. توليد بيانات المعاينة المباشرة (Base64) بدقة وتغذيتها لمستعرض PDF.
  7. تأكيد الأرشفة الصريحة ونقل المستند إلى المستودع المحلي الدائم وفهرسته في FTS5.
  8. استقرار البيانات عبر إعادة تشغيل التطبيق.
- **جناح اختبارات النواة المحلية (Native Core Test Suite)**: 9 من 9 مراحل بنجاح 100%.
- **جناح اختبارات المكونات المساعدة (Python Test Suite)**: 48 من 48 اختباراً بنجاح 100%.
- **اختبار بيئة Electron الحية (Electron Runtime Live Verification)**: 5 من 5 اختبارات بنجاح 100%.

---

**الفرع المصدري**: `develop`  
**الوسم المصدري (Git Tag)**: `v2.0.1`  

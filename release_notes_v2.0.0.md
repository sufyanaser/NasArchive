# NAS Archive Native 2.0.0 — التحول المكتبي المستقل لنظام Windows 11

يسرنا إطلاق الإصدار الرئيسي **NAS Archive Native 2.0.0**، والذي يمثل تحولاً معمارياً كاملاً للنظام ليصبح تطبيقاً مكتبياً أصيلاً ومستقلاً تماماً على بيئة **Windows 11**، دون أي اعتماد على Docker Desktop أو محركات الحاويات أو WSL أو فتح متصفح خارجي أو فتح منافذ شبكية محلية غير مؤمنة.

---

### 📦 حزمة التثبيت الرسمية وبيانات التحقق

| الملف | الوصف | الحجم | بصمة التشفير (SHA-256) |
| :--- | :--- | :---: | :--- |
| **`NAS Archive Setup 2.0.0.exe`** | مثبت نظام Windows 11 x64 المستقل (NSIS) | 122.45 MB | `1C89AD24F37DEEB6A5EBE9F96EFBD641C35F2CDCF63D8C3348055761FA6F0E80` |

---

### ✨ أبرز مميزات التحديث المعماري (v2.0.0)

1. **الاستقلال الكامل عن Docker وWSL**:
   - لا يتطلب تشغيل التطبيق تثبيت Docker Desktop أو WSL أو تشغيل أي أوامر طرفية أو فتح المتصفح.
   - تشغيل فوري وبدء بارد (Cold Startup) في أقل من ثانية واحدة.
2. **محرك النواة المحلية (Native Core Engine)**:
   - **قاعدة بيانات SQLite 3.47+ مدمجة** بوضع WAL المتقدم مع تفعيل المفاتيح الأجنبية والمعاملات الذرية.
   - **محرك البحث النصي العربي المتقدم (SQLite FTS5)** مع تطبيع ذكي للنصوص العربية وإزالة التشكيل وتوحيد الألف والياء والتاء المربوطة.
   - **محرك التعرف الضوئي على النصوص (OCR Engine)** للغتين العربية والإنجليزية مع تضمين نماذج `ara.traineddata` و`eng.traineddata` داخل حزمة التوزيع.
   - **الحفاظ الكامل على أرقام الكتب والقيود الإدارية**: حفظ الأصفار البادئة والشرطات كنصوص صريحة (`0042/ص-2026` و`00987-ق`).
3. **تكامل الماسح الضوئي الفيزيائي (NAPS2 / WIA)**:
   - دعم مباشر للماسح المتصل `EPSON WF-C5890 Series` عبر مشغل WIA والشبكة.
   - مسار تشغيل آمن ومخفي بالكامل دون ظهور أي نوافذ طرفية سوداء.
   - سير عمل المسح ثنائي المراحل: (المرحلة A: المعاينة الفورية بالحجم الكامل — المرحلة B: الأرشفة الصريحة وفهرسة FTS5).
4. **أمان قنوات الاتصال (Zero Localhost Ports)**:
   - إغلاق كافة المنافذ الشبكية المحلية المفتوحة (لا يوجد منفذ 8000 ولا منفذ 8001).
   - توجيه جميع عمليات الواجهة عبر قنوات Electron IPC المعزولة والآمنة (`contextIsolation: true`, `nodeIntegration: false`).
5. **محرك النسخ الاحتياطي والاستعادة الذاتي**:
   - توليد حزم نسخ احتياطي متكاملة للملفات الأصلية والمؤرشفة وقاعدة البيانات مع بيان فحص المطابقة والتحقق من بصمات SHA-256 بنسبة 100%.

---

### 🔄 تعليمات الترحيل من Paperless-ngx القديم (Migration Instructions)

- **الترحيل التلقائي الفوري**: يقوم التطبيق تلقائياً عند التشغيل الأول باكتشاف مستندات Paperless السابقة وترحيلها بالكامل إلى قاعدة بيانات SQLite المحلية ومخزن المستندات الدائم.
- **تطابق البيانات بنسبة 100%**: تم التحقق آلياً من ترحيل كافة الوثائق الـ 12 السابقة ومطابقة بصمات SHA-256 للملفات الأصلية والنسخ المؤرشفة وبيانات الحقول المخصصة.

---

### 🛡️ تعليمات الأمان والاسترجاع الاحتياطي (Rollback Instructions)

- تم إيقاف حاويات Docker القديمة بأمان (`docker compose stop`).
- تم الاحتفاظ التام بكافة وحدات التخزين ومجلدات الحاويات وقواعد البيانات السابقة دون أي تعديل أو حذف كنسخة استرجاع احتياطية دائمة.
- في حال الرغبة بالرجوع للبيئة السابقة، يمكن تشغيل الأمر `docker compose start` دون أي فقدان للبيانات السابقة.

---

### ⚠️ الحدود والمتطلبات الخارجية (Known Limitations)

1. **الماسح الضوئي الفيزيائي**: تم التحقق والاختبار الفعلي للماسح المتصل `EPSON WF-C5890 Series`. في حال استخدام ماسح ضوئي آخر، يُشترط دعمه لمشغلات WIA القياسية في نظام Windows.
2. **الربط السحابي مع Google Drive وSheets**: المكونات البرمجية جاهزة ومختبرة محلياً 100%. للربط المباشر مع سحابة Google، يلزم وضع ملف الاعتماد في `runtime/google_credentials.json` وضبط معرفات المجلد وجدول البيانات.

---

**الالتزام المصدري (Git Commit)**: `492ed53`  
**الفرع المصدري**: `develop`  

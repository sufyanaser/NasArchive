# NAS Archive Native 2.1.0 — استعادة وظائف الأرشيف الكاملة ونظام التنبيهات الحديث

الإصدار الشامل **NAS Archive Native 2.1.0** لاستعادة كامل تجربة إدارة الأرشيف المتقدمة التي كانت متاحة سابقاً في Paperless، وتصحيح سلوك محرر المستندات بالكامل مع إعادة تصميم نظام التنبيهات والحوارات بنمط macOS الحديث، مع الحفاظ الصارم على البنية المحلية المستقلة بنسبة 100% دون Docker أو WSL أو منافذ خوادم ويب متصفح.

---

### 📦 حزمة التثبيت الرسمية وبيانات التحقق (Official Installer)

| الملف | الوصف | الحجم | بصمة التشفير (SHA-256) |
| :--- | :--- | :---: | :--- |
| **`NAS Archive Setup 2.1.0.exe`** | مثبت نظام Windows 11 x64 المستقل (NSIS) | 122.47 MB (128,419,796 بايت) | `9CE66C9A1F9EE4E2BCD213B832A366CAC78CE03A89A7C6B9177F2037C0744913` |

---

### 📊 مصفوفة المقارنة واستعادة الوظائف (Feature Gap Resolution Matrix)

| الوظيفة / الخاصية | بيئة Paperless السابقة | NAS Archive Native 2.0.x | NAS Archive Native 2.1.0 (الحالي) |
| :--- | :---: | :---: | :---: |
| **محرك التشغيل** | حاويات Docker Desktop / WSL | تطبيق محلي مستقل | **تطبيق محلي أصيل 100% بدون Docker/WSL** |
| **سلوك حفظ محرر المستندات** | حفظ وإغلاق سلس | ينبثق alert ويبقى مفتوحاً | **تحقق، التزام بقاعدة البيانات، إغلاق تلقائي، إشعار هادئ** |
| **نظام التنبيهات** | إشعارات الويب الافتراضية | نوافذ OS alert/confirm حاجزة | **نظام macOS Toasts عائم وغير حاجب + حوارات تأكيد مخصصة** |
| **أنماط عرض الأرشيف** | شبكة، جدول، قائمة | شبكة فقط | **عرض الشبكة (Grid) + جدول تفصيلي (Table) + قائمة مدمجة (List)** |
| **التحديد والعمليات الجماعية** | تحديد صفحات والكل وإجراءات مجمعة | غير متاح | **تحديد الصفحة والكل، شريط إجراءات جماعي (حذف، استعادة، وسوم، اعتماد)** |
| **شريط التصفية والفرز المتقدم** | متعدد الأبعاد مع شرائح نشطة | فلترة أقسام وبحث فقط | **شريط فلترة منسدل (وسوم، مراسلون، أنواع، مسارات، تواريخ، حالة) + شرائح نشطة** |
| **طرق العرض المحفوظة** | متاحة وقابلة للتخصيص | غير متاح | **حفظ العرض الحالي، اختيار طرق العرض المحفوظة، وتطبيقها بنقرة واحدة** |
| **سلة المهملات والحذف الآمن** | غير مدعوم محلياً | حذف فوري مباشر | **حذف ناعم (Soft-Delete) + استعادة + تفريغ السلة وحذف نهائي** |
| **إدارة السمات والتصنيفات** | واجهات CRUD منفصلة | مقتصرة على 12 وسماً | **واجهة إدارة موحدة لـ (الوسوم، المراسلين، أنواع المستندات، مسارات التخزين، الحقول)** |
| **سير العمل والتشغيل الآلي** | قواعد سير عمل Paperless | غير متاح | **إدارة قواعد سير العمل (Workflows) مع تقييم الشروط وتنفيذ الإجراءات** |
| **سجلات النظام والمهام** | سجلات Docker و Celery | غير متاح بالواجهة | **سجلات نظام تدقيقية تفاعلية + سجل المهام الخلفية المنجزة والجارية** |
| **المستخدمون المحليون** | مستخدمو Django | مستخدم داخلي ضمني | **إدارة الحسابات المحلية وإظهار صلاحيات المدير nasadmin** |

---

### 🛠️ الإصلاحات المعمارية والبرمجية الأساسية

#### 1. تصحيح سلوك زر الحفظ في محرر المستندات (Document Editor Save Workflow)
- **التحقق المسبق والتحقق من عدم التغيير (Validation & Dirty-Checking)**: فحص صحة الحقول قبل الإرسال، ومقارنة الحالة الحالية بالحالة الأصلية للنموذج لتجنب عمليات الكتابة غير الضرورية.
- **تأكيد الالتزام بالمعاملة في SQLite**: إجراء الحفظ عبر معاملة ذرية متكاملة وتحديث فهارس البحث FTS5.
- **الإغلاق التلقائي للمحرر**: إغلاق نافذة المحرر فور نجاح العملية والعودة السلسة لطريقة عرض الأرشيف السابقة مع تحديث البطاقة والمستندات.
- **إشعار نجاح هادئ**: إشعار المستخدم عبر إشعار Toast أخضر هادئ ومؤقت دون إيقاف سير العمل أو الحاجة لأي نقرة إضافية.
- **الحماية عند حدوث أخطاء**: في حال حدوث أي خطأ، تبقى نافذة المحرر مفتوحة مع الاحتفاظ بكافة المدخلات والتعديلات التي أجراها المستخدم، مع إظهار إشعار خطأ يتيح زر "إعادة المحاولة" (Retry).

#### 2. نظام التنبيهات وحوارات التأكيد الجديد (macOS Floating Notifications)
- **إلغاء حوارات المتصفح البدائية**: إزالة كافة استدعاءات `window.alert` و `window.confirm` نهائياً.
- **إشعارات عائمة (Floating Toasts)**: تظهر في الزاوية العلوية بمظهر macOS الزجاجي الأنيق بأربعة أنماط (نجاح، خطأ، تحذير، معلومات) مع دعم إمكانية الوصول (ARIA Live Roles).
- **منع التكرار (Deduplication) والإخفاء التلقائي**: منع تكرار نفس الرسالة خلال 1.5 ثانية، والإغلاق التلقائي بعد فترة محددة أو عند النقر.
- **حوارات تأكيد عصرية للعمليات الخطرة (Confirm Dialogs)**: نافذة حوار مخصصة بدقة تدعم الوعود البرمجية `Promise-based` لتأكيد عمليات الحذف والتفريغ مع تلوين أزرار الخطر باللون الأحمر.

#### 3. ترقية قاعدة البيانات المحلية (Migration 2)
- **إنشاء الجداول الممتدة**:
  - `storage_paths`: قوالب مسارات التخزين المنظمة.
  - `saved_views`: طرق العرض المخصصة وقواعد الفرز والتصفية.
  - `workflows`: قواعد التشغيل الآلي والتصنيف المشروط.
  - `app_tasks`: سجل المهام الخلفية وحالتها.
  - `app_logs`: سجلات التدقيق والأخطاء والعمليات.
  - `local_users`: الحسابات المحلية وصلاحياتها.
- **دعم الحذف الناعم (Soft Delete)**: إضافة عمود `deleted_at` وعمود `storage_path_id` إلى جدول `documents` مع عزل المستندات المحذوفة مؤقتاً عن نتائج البحث الاعتيادية وفهارس FTS5.
- **النسخ الاحتياطي التلقائي قبل الترقية**: يقوم نظام `db.js` بإنشاء نسخة احتياطية موثقة تلقائياً في مجلد `pre_migration_backups/` قبل تنفيذ أي ترحيل هيكلي.

---

### 🧪 نتائج الاختبارات الآلية الشاملة (Test Verification)

1. **جناح اختبارات الانحدار الشامل لاستعادة الأرشيف (12/12 بنسبة 100%)**:
   - `[SCENARIO 1]` حفظ محرر المستندات والإغلاق التلقائي — **PASS**
   - `[SCENARIO 2]` الحفاظ على مدخلات المستخدم عند فشل الحفظ — **PASS**
   - `[SCENARIO 3]` استمرارية وتطابق البيانات الوصفية (Metadata Persistence) — **PASS**
   - `[SCENARIO 4]` العمليات الكاملة للتصنيفات والسمات (Classification CRUD) — **PASS**
   - `[SCENARIO 5]` الفلاتر المتعددة والبحث المشترك (Combined Filters) — **PASS**
   - `[SCENARIO 6]` طرق العرض المحفوظة (Saved Views Lifecycle) — **PASS**
   - `[SCENARIO 7]` العمليات الجماعية على المستندات (Bulk Actions) — **PASS**
   - `[SCENARIO 8]` معاينة المستندات المباشرة عبر IPC readBinary — **PASS**
   - `[SCENARIO 9]` ترحيل قاعدة البيانات والنسخ الاحتياطي التلقائي (Migration 2) — **PASS**
   - `[SCENARIO 10]` استقرار البيانات واستمراريتها عبر إعادة تشغيل التطبيق — **PASS**
   - `[SCENARIO 11]` سلوك نظام التنبيهات وحوارات التأكيد (Notifications) — **PASS**
   - `[SCENARIO 12]` دورة حياة سلة المهملات والاستعادة والحذف النهائي — **PASS**

2. **جناح اختبارات النواة المحلية (Native Core Suite)**: 9 من 9 مراحل بنجاح 100%.
3. **جناح اختبارات فحص الماسح ومعاينة PDF (Scanner & Preview Suite)**: 8 من 8 مراحل بنجاح 100%.
4. **جناح اختبارات بايثون للمكونات المساعدة (Python Backend Suite)**: 48 من 48 اختباراً بنجاح 100%.
5. **التحقق الحي لبيئة تشغيل Electron (Electron Runtime Live Verification)**: 5 من 5 اختبارات بنجاح 100%.

---

**المستودع المصدري**: `https://github.com/sufyanaser/NasArchive`  
**الفرع المعتمد**: `develop`  
**الوسم المصدري (Git Tag)**: `v2.1.0`  
**رقم التخريج (Commit Hash)**: `a66fab0`
